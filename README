You should be able to get running just by doing:

    virtualenv --python=python2 .
    source bin/activate
    pip install -r requirements.txt

Then you can configure the bot and start it with

    ./midori.py
